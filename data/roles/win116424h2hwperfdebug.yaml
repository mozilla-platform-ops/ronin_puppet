---

win-worker:
  function: "tester"
  generic_worker:
    client_id: "project/releng/generic-worker/win11-64-24h2-hw-perf-debug"

  # Logging
  # Logging level options debug, verbose, or restricted.
  log:
    level: "verbose"
