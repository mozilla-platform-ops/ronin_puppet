---

win-worker:
  function: "tester"

  # Mozilla profile
  mozilla_profile:
    source: "https://roninpuppetassets.blob.core.windows.net/binaries/mozilla/mozprofilerprobe.mof"
    local:  "%{facts.custom_win_roninprogramdata}\\mozprofilerprobe.mof"

  vac:
    version: '464'

  # Logging
  # Logging level options debug, verbose, or restricted.
  log:
    level: "restricted"

  ## Use to overwrite values in the Windows yaml file.
  ## Replace windows. with win-worker.variant.
  variant:

# Taskcluster worker configuration - build from source
#roles_profiles::profiles::windows_worker_runner::build_from_source: 'true'
#roles_profiles::profiles::windows_worker_runner::taskcluster_ref: 'matt-boris/gwWindowsDetachedConsole'
